let names:string[] = ["Aseed", "Ammar", "Waleed", "Moudood"];

for (const namesKey of names) {
    if (namesKey == "Waleed"){
        console.log("FYP Partner: " + namesKey);
    }else {
        console.log("Normies: " + namesKey);
    }
}
