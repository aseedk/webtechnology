var names = ["Aseed", "Ammar", "Waleed", "Moudood"];
for (var _i = 0, names_1 = names; _i < names_1.length; _i++) {
    var namesKey = names_1[_i];
    if (namesKey == "Waleed") {
        console.log("FYP Partner : " + namesKey);
    }
    else {
        console.log("Normies: " + namesKey);
    }
}
